dataset:
  root: dataset_sr
  batch_size: 16
  val_split: 0.8

training:
  epochs: 20
  lr: 0.0001

loss:
  perceptual_weight: 0.01

system:
  device: auto
  seed: 42

save:
  path: saved_models/unet_sr.pth